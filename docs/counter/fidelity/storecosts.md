# Fidelity — Store file (`storecosts`)

Route `/dashboard/stores/cmexd4zia0001jr04ljkdt9na` · manifest status **counter** · measured 2026-08-31

Generated by `npm run fidelity:report` from what `npm run fidelity` measured. Do not edit by hand — re-run it.

## fidelity — desk

**The prototype renders 39 landmarks on this page. We render 30.**

| landmark | prototype | ours |
|---|---:|---:|
| `.btn` | 10 | 2 |
| `.btnrow` | 2 | 1 |
| `.kv` | 2 | 2 |
| `.sec` | 8 | 8 |
| `.sec__body` | 7 | 7 |
| `.sec__head` | 8 | 8 |
| `.strip` | 1 | 1 |
| `.tbl` | 1 | 1 |

Structure: 9 missing, 0 extra, 30 matched. Rendering (light): 0 property differences on the 30 landmarks present on both sides.

### Missing — the prototype has these and we do not

- `#5 .btnrow`
- `#6 .btn`
- `#7 .btn`
- `#31 .btn`
- `#32 .btn`
- `#33 .btn`
- `#34 .btn`
- `#37 .btn`
- `#38 .btn`

### Dark mode — asserted on its own terms, never against the prototype

210 of 297 elements paint a colour of their own; 261 were swept (every element that paints or carries text, not only the 30 landmarks — see the "FIX ROUND 1" note in landmarks.ts). Checked against 47 resolved `--ct-*` tokens. 0 defects.


## fidelity-mobile — phone

**The prototype renders 9 landmarks on this page. We render 9.**

| landmark | prototype | ours |
|---|---:|---:|
| `.mlist` | 2 | 2 |
| `.mstrip` | 1 | 1 |
| `.sec` | 2 | 2 |
| `.sec__body` | 2 | 2 |
| `.sec__head` | 2 | 2 |

Structure: 0 missing, 0 extra, 9 matched. Rendering (light): 0 property differences on the 9 landmarks present on both sides.

### Dark mode — asserted on its own terms, never against the prototype

114 of 161 elements paint a colour of their own; 136 were swept (every element that paints or carries text, not only the 9 landmarks — see the "FIX ROUND 1" note in landmarks.ts). Checked against 47 resolved `--ct-*` tokens. 0 defects.

