# Fidelity — An ingredient (`ingredient`)

Route `/dashboard/ingredients/cmo58ubkh00143nu9b0c14gi1` · manifest status **counter** · measured 2026-08-31

Generated by `npm run fidelity:report` from what `npm run fidelity` measured. Do not edit by hand — re-run it.

## fidelity — desk

**The prototype renders 12 landmarks on this page. We render 12.**

| landmark | prototype | ours |
|---|---:|---:|
| `.btn` | 1 | 1 |
| `.ch` | 1 | 1 |
| `.sec` | 3 | 3 |
| `.sec__body` | 1 | 1 |
| `.sec__head` | 3 | 3 |
| `.strip` | 1 | 1 |
| `.tbl` | 2 | 2 |

Structure: 0 missing, 0 extra, 12 matched. Rendering (light): 0 property differences on the 12 landmarks present on both sides.

### Dark mode — asserted on its own terms, never against the prototype

138 of 233 elements paint a colour of their own; 175 were swept (every element that paints or carries text, not only the 12 landmarks — see the "FIX ROUND 1" note in landmarks.ts). Checked against 46 resolved `--ct-*` tokens. 0 defects.


## fidelity-mobile — phone

**The prototype renders 9 landmarks on this page. We render 9.**

| landmark | prototype | ours |
|---|---:|---:|
| `.ch` | 1 | 1 |
| `.mlist` | 1 | 1 |
| `.mstrip` | 1 | 1 |
| `.sec` | 2 | 2 |
| `.sec__body` | 2 | 2 |
| `.sec__head` | 2 | 2 |

Structure: 0 missing, 0 extra, 9 matched. Rendering (light): 0 property differences on the 9 landmarks present on both sides.

### Dark mode — asserted on its own terms, never against the prototype

108 of 174 elements paint a colour of their own; 134 were swept (every element that paints or carries text, not only the 9 landmarks — see the "FIX ROUND 1" note in landmarks.ts). Checked against 46 resolved `--ct-*` tokens. 0 defects.

