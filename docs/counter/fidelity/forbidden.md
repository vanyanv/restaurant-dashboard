# Fidelity — No access (`forbidden`)

Route `/dashboard/forbidden` · manifest status **counter** · measured 2026-09-01

Generated by `npm run fidelity:report` from what `npm run fidelity` measured. Do not edit by hand — re-run it.

## fidelity — desk

**The prototype renders 7 landmarks on this page. We render 7.**

| landmark | prototype | ours |
|---|---:|---:|
| `.btn` | 2 | 2 |
| `.btnrow` | 1 | 1 |
| `.empty` | 1 | 1 |
| `.sec` | 1 | 1 |
| `.sec__body` | 1 | 1 |
| `.sec__head` | 1 | 1 |

Structure: 0 missing, 0 extra, 7 matched. Rendering (light): 0 property differences on the 7 landmarks present on both sides.

### Dark mode — asserted on its own terms, never against the prototype

9 of 14 elements paint a colour of their own; 13 were swept (every element that paints or carries text, not only the 7 landmarks — see the "FIX ROUND 1" note in landmarks.ts). Checked against 47 resolved `--ct-*` tokens. 0 defects.


## fidelity-mobile — phone

**The prototype renders 2 landmarks on this page. We render 2.**

| landmark | prototype | ours |
|---|---:|---:|
| `.empty` | 1 | 1 |
| `.mbtn` | 1 | 1 |

Structure: 0 missing, 0 extra, 2 matched. Rendering (light): 0 property differences on the 2 landmarks present on both sides.

### Dark mode — asserted on its own terms, never against the prototype

3 of 5 elements paint a colour of their own; 4 were swept (every element that paints or carries text, not only the 2 landmarks — see the "FIX ROUND 1" note in landmarks.ts). Checked against 47 resolved `--ct-*` tokens. 0 defects.

