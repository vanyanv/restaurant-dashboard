# Fidelity — Product mix (`productmix`)

Route `/dashboard/product-mix` · manifest status **counter** · measured 2026-08-31

Generated by `npm run fidelity:report` from what `npm run fidelity` measured. Do not edit by hand — re-run it.

## fidelity — desk

**The prototype renders 12 landmarks on this page. We render 12.**

| landmark | prototype | ours |
|---|---:|---:|
| `.btn` | 1 | 1 |
| `.ch` | 1 | 1 |
| `.sec` | 3 | 3 |
| `.sec__body` | 2 | 2 |
| `.sec__head` | 3 | 3 |
| `.strip` | 1 | 1 |
| `.tbl` | 1 | 1 |

Structure: 0 missing, 0 extra, 12 matched. Rendering (light): 0 property differences on the 12 landmarks present on both sides.

### Dark mode — asserted on its own terms, never against the prototype

168 of 281 elements paint a colour of their own; 216 were swept (every element that paints or carries text, not only the 12 landmarks — see the "FIX ROUND 1" note in landmarks.ts). Checked against 46 resolved `--ct-*` tokens. 0 defects.


## fidelity-mobile — phone

**The prototype renders 5 landmarks on this page. We render 5.**

| landmark | prototype | ours |
|---|---:|---:|
| `.ch` | 1 | 1 |
| `.mstrip` | 1 | 1 |
| `.sec` | 1 | 1 |
| `.sec__body` | 1 | 1 |
| `.sec__head` | 1 | 1 |

Structure: 0 missing, 0 extra, 5 matched. Rendering (light): 0 property differences on the 5 landmarks present on both sides.

### Dark mode — asserted on its own terms, never against the prototype

102 of 166 elements paint a colour of their own; 130 were swept (every element that paints or carries text, not only the 5 landmarks — see the "FIX ROUND 1" note in landmarks.ts). Checked against 46 resolved `--ct-*` tokens. 0 defects.

