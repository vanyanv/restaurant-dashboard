# Fidelity — Login (`login`)

Route `/login` · manifest status **counter** · measured 2026-09-01

Generated by `npm run fidelity:report` from what `npm run fidelity` measured. Do not edit by hand — re-run it.

## fidelity — desk

**The prototype renders 2 landmarks on this page. We render 1.**

| landmark | prototype | ours |
|---|---:|---:|
| `.btn` | 2 | 1 |

Structure: 1 missing, 0 extra, 1 matched. Rendering (light): 0 property differences on the 1 landmarks present on both sides.

### Missing — the prototype has these and we do not

- `#1 .btn`

### Dark mode — asserted on its own terms, never against the prototype

19 of 28 elements paint a colour of their own; 21 were swept (every element that paints or carries text, not only the 1 landmarks — see the "FIX ROUND 1" note in landmarks.ts). Checked against 47 resolved `--ct-*` tokens. 0 defects.


## fidelity-mobile — phone

**The prototype renders 2 landmarks on this page. We render 1.**

| landmark | prototype | ours |
|---|---:|---:|
| `.mbtn` | 2 | 1 |

Structure: 1 missing, 0 extra, 1 matched. Rendering (light): 0 property differences on the 1 landmarks present on both sides.

### Missing — the prototype has these and we do not

- `#1 .mbtn`

### Dark mode — asserted on its own terms, never against the prototype

9 of 13 elements paint a colour of their own; 9 were swept (every element that paints or carries text, not only the 1 landmarks — see the "FIX ROUND 1" note in landmarks.ts). Checked against 47 resolved `--ct-*` tokens. 0 defects.

