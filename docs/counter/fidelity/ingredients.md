# Fidelity — Ingredients (`ingredients`)

Route `/dashboard/ingredients` · manifest status **counter** · measured 2026-09-01

Generated by `npm run fidelity:report` from what `npm run fidelity` measured. Do not edit by hand — re-run it.

## fidelity — desk

**The prototype renders 34 landmarks on this page. We render 34.**

| landmark | prototype | ours |
|---|---:|---:|
| `.btn` | 11 | 11 |
| `.ch` | 1 | 1 |
| `.qitem` | 2 | 2 |
| `.queue` | 1 | 1 |
| `.sec` | 6 | 6 |
| `.sec__body` | 3 | 3 |
| `.sec__head` | 6 | 6 |
| `.strip` | 1 | 1 |
| `.tbl` | 3 | 3 |

Structure: 0 missing, 0 extra, 34 matched. Rendering (light): 0 property differences on the 34 landmarks present on both sides.

### Dark mode — asserted on its own terms, never against the prototype

319 of 490 elements paint a colour of their own; 392 were swept (every element that paints or carries text, not only the 34 landmarks — see the "FIX ROUND 1" note in landmarks.ts). Checked against 47 resolved `--ct-*` tokens. 0 defects.


## fidelity-mobile — phone

**The prototype renders 10 landmarks on this page. We render 10.**

| landmark | prototype | ours |
|---|---:|---:|
| `.ch` | 1 | 1 |
| `.mbtn` | 1 | 1 |
| `.mlist` | 1 | 1 |
| `.mstrip` | 1 | 1 |
| `.sec` | 2 | 2 |
| `.sec__body` | 2 | 2 |
| `.sec__head` | 2 | 2 |

Structure: 0 missing, 0 extra, 10 matched. Rendering (light): 0 property differences on the 10 landmarks present on both sides.

### Dark mode — asserted on its own terms, never against the prototype

34 of 111 elements paint a colour of their own; 81 were swept (every element that paints or carries text, not only the 10 landmarks — see the "FIX ROUND 1" note in landmarks.ts). Checked against 47 resolved `--ct-*` tokens. 0 defects.

