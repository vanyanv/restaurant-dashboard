# Fidelity — Ingredients (`ingredients`)

Route `/dashboard/ingredients` · manifest status **counter** · measured 2026-08-31

Generated by `npm run fidelity:report` from what `npm run fidelity` measured. Do not edit by hand — re-run it.

## fidelity — desk

**The prototype renders 34 landmarks on this page. We render 34.**

| landmark | prototype | ours |
|---|---:|---:|
| `.btn` | 11 | 11 |
| `.ch` | 1 | 1 |
| `.qitem` | 2 | 2 |
| `.queue` | 1 | 1 |
| `.sec` | 6 | 6 |
| `.sec__body` | 3 | 3 |
| `.sec__head` | 6 | 6 |
| `.strip` | 1 | 1 |
| `.tbl` | 3 | 3 |

Structure: 0 missing, 0 extra, 34 matched. Rendering (light): 0 property differences on the 34 landmarks present on both sides.

### Dark mode — asserted on its own terms, never against the prototype

319 of 485 elements paint a colour of their own; 388 were swept (every element that paints or carries text, not only the 34 landmarks — see the "FIX ROUND 1" note in landmarks.ts). Checked against 47 resolved `--ct-*` tokens. 0 defects.


## fidelity-mobile — phone

**The prototype renders 9 landmarks on this page. We render 9.**

| landmark | prototype | ours |
|---|---:|---:|
| `.ch` | 1 | 1 |
| `.mlist` | 1 | 1 |
| `.mstrip` | 1 | 1 |
| `.sec` | 2 | 2 |
| `.sec__body` | 2 | 2 |
| `.sec__head` | 2 | 2 |

Structure: 0 missing, 0 extra, 9 matched. Rendering (light): 0 property differences on the 9 landmarks present on both sides.

### Dark mode — asserted on its own terms, never against the prototype

121 of 215 elements paint a colour of their own; 171 were swept (every element that paints or carries text, not only the 9 landmarks — see the "FIX ROUND 1" note in landmarks.ts). Checked against 47 resolved `--ct-*` tokens. 0 defects.

