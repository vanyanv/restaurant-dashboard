# Fidelity — New store (`storeedit`)

Route `/dashboard/stores/new` · manifest status **counter** · measured 2026-08-31

Generated by `npm run fidelity:report` from what `npm run fidelity` measured. Do not edit by hand — re-run it.

## fidelity — desk

**The prototype renders 16 landmarks on this page. We render 16.**

| landmark | prototype | ours |
|---|---:|---:|
| `.btn` | 2 | 2 |
| `.btnrow` | 1 | 1 |
| `.qitem` | 3 | 3 |
| `.queue` | 1 | 1 |
| `.sec` | 3 | 3 |
| `.sec__body` | 2 | 2 |
| `.sec__head` | 3 | 3 |
| `.tbl` | 1 | 1 |

Structure: 0 missing, 0 extra, 16 matched. Rendering (light): 0 property differences on the 16 landmarks present on both sides.

### Dark mode — asserted on its own terms, never against the prototype

62 of 99 elements paint a colour of their own; 80 were swept (every element that paints or carries text, not only the 16 landmarks — see the "FIX ROUND 1" note in landmarks.ts). Checked against 47 resolved `--ct-*` tokens. 0 defects.


## fidelity-mobile — phone

**The prototype renders 4 landmarks on this page. We render 4.**

| landmark | prototype | ours |
|---|---:|---:|
| `.mlist` | 1 | 1 |
| `.sec` | 1 | 1 |
| `.sec__body` | 1 | 1 |
| `.sec__head` | 1 | 1 |

Structure: 0 missing, 0 extra, 4 matched. Rendering (light): 0 property differences on the 4 landmarks present on both sides.

### Dark mode — asserted on its own terms, never against the prototype

108 of 146 elements paint a colour of their own; 123 were swept (every element that paints or carries text, not only the 4 landmarks — see the "FIX ROUND 1" note in landmarks.ts). Checked against 47 resolved `--ct-*` tokens. 0 defects.

