# Fidelity — Menu (`menuhub`)

Route `/dashboard/menu` · manifest status **counter** · measured 2026-08-31

Generated by `npm run fidelity:report` from what `npm run fidelity` measured. Do not edit by hand — re-run it.

## fidelity — desk

**The prototype renders 12 landmarks on this page. We render 12.**

| landmark | prototype | ours |
|---|---:|---:|
| `.btn` | 1 | 1 |
| `.qitem` | 3 | 3 |
| `.queue` | 1 | 1 |
| `.sec` | 2 | 2 |
| `.sec__body` | 2 | 2 |
| `.sec__head` | 2 | 2 |
| `.strip` | 1 | 1 |

Structure: 0 missing, 0 extra, 12 matched. Rendering (light): 0 property differences on the 12 landmarks present on both sides.

### Dark mode — asserted on its own terms, never against the prototype

128 of 195 elements paint a colour of their own; 159 were swept (every element that paints or carries text, not only the 12 landmarks — see the "FIX ROUND 1" note in landmarks.ts). Checked against 46 resolved `--ct-*` tokens. 0 defects.


## fidelity-mobile — phone

**The prototype renders 5 landmarks on this page. We render 5.**

| landmark | prototype | ours |
|---|---:|---:|
| `.mlist` | 1 | 1 |
| `.mstrip` | 1 | 1 |
| `.sec` | 1 | 1 |
| `.sec__body` | 1 | 1 |
| `.sec__head` | 1 | 1 |

Structure: 0 missing, 0 extra, 5 matched. Rendering (light): 0 property differences on the 5 landmarks present on both sides.

### Dark mode — asserted on its own terms, never against the prototype

102 of 141 elements paint a colour of their own; 116 were swept (every element that paints or carries text, not only the 5 landmarks — see the "FIX ROUND 1" note in landmarks.ts). Checked against 46 resolved `--ct-*` tokens. 0 defects.

