# Fidelity — Analytics (`analytics`)

Route `/dashboard/analytics` · manifest status **counter** · measured 2026-09-01

Generated by `npm run fidelity:report` from what `npm run fidelity` measured. Do not edit by hand — re-run it.

## fidelity — desk

**The prototype renders 25 landmarks on this page. We render 23.**

| landmark | prototype | ours |
|---|---:|---:|
| `.band` | 4 | 3 |
| `.blt` | 2 | 2 |
| `.btn` | 1 | 1 |
| `.ch` | 3 | 3 |
| `.drill` | 1 | 1 |
| `.sec` | 3 | 3 |
| `.sec__body` | 3 | 3 |
| `.sec__head` | 3 | 3 |
| `.sp` | 3 | 2 |
| `.strip` | 1 | 1 |
| `.tbl` | 1 | 1 |

Structure: 2 missing, 0 extra, 23 matched. Rendering (light): 2 property differences on the 23 landmarks present on both sides.

### Missing — the prototype has these and we do not

- `#9 .sp`
- `#10 .band`

### Rendering differences

| landmark | property | prototype | ours |
|---|---|---|---|
| `#1 .strip` | `data-n` | `4` | `3` |
| `#1 .strip` | `grid-template-columns` | `4 tracks` | `3 tracks` |

### Dark mode — asserted on its own terms, never against the prototype

176 of 456 elements paint a colour of their own; 316 were swept (every element that paints or carries text, not only the 23 landmarks — see the "FIX ROUND 1" note in landmarks.ts). Checked against 47 resolved `--ct-*` tokens. 0 defects.


## fidelity-mobile — phone

**The prototype renders 11 landmarks on this page. We render 11.**

| landmark | prototype | ours |
|---|---:|---:|
| `.band` | 1 | 1 |
| `.blt` | 1 | 1 |
| `.ch` | 2 | 2 |
| `.mstrip` | 1 | 1 |
| `.sec` | 2 | 2 |
| `.sec__body` | 2 | 2 |
| `.sec__head` | 2 | 2 |

Structure: 0 missing, 0 extra, 11 matched. Rendering (light): 0 property differences on the 11 landmarks present on both sides.

### Dark mode — asserted on its own terms, never against the prototype

44 of 166 elements paint a colour of their own; 114 were swept (every element that paints or carries text, not only the 11 landmarks — see the "FIX ROUND 1" note in landmarks.ts). Checked against 47 resolved `--ct-*` tokens. 0 defects.

