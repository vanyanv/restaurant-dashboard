# Fidelity — Settings (`settings`)

Route `/dashboard/settings` · manifest status **counter** · measured 2026-08-31

Generated by `npm run fidelity:report` from what `npm run fidelity` measured. Do not edit by hand — re-run it.

## fidelity — desk

**The prototype renders 28 landmarks on this page. We render 21.**

| landmark | prototype | ours |
|---|---:|---:|
| `.btn` | 6 | 1 |
| `.btnrow` | 2 | 0 |
| `.kv` | 1 | 1 |
| `.sec` | 6 | 6 |
| `.sec__body` | 6 | 6 |
| `.sec__head` | 6 | 6 |
| `.tbl` | 1 | 1 |

Structure: 7 missing, 0 extra, 21 matched. Rendering (light): 0 property differences on the 21 landmarks present on both sides.

### Missing — the prototype has these and we do not

- `#10 .btn`
- `#11 .btnrow`
- `#12 .btn`
- `#24 .btnrow`
- `#25 .btn`
- `#26 .btn`
- `#27 .btn`

### Dark mode — asserted on its own terms, never against the prototype

104 of 180 elements paint a colour of their own; 146 were swept (every element that paints or carries text, not only the 21 landmarks — see the "FIX ROUND 1" note in landmarks.ts). Checked against 47 resolved `--ct-*` tokens. 0 defects.


## fidelity-mobile — phone

**The prototype renders 8 landmarks on this page. We render 8.**

| landmark | prototype | ours |
|---|---:|---:|
| `.mhead` | 1 | 1 |
| `.mlist` | 1 | 1 |
| `.sec` | 2 | 2 |
| `.sec__body` | 2 | 2 |
| `.sec__head` | 2 | 2 |

Structure: 0 missing, 0 extra, 8 matched. Rendering (light): 0 property differences on the 8 landmarks present on both sides.

### Dark mode — asserted on its own terms, never against the prototype

126 of 178 elements paint a colour of their own; 145 were swept (every element that paints or carries text, not only the 8 landmarks — see the "FIX ROUND 1" note in landmarks.ts). Checked against 47 resolved `--ct-*` tokens. 0 defects.

