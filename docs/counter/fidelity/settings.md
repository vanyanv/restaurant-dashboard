# Fidelity — Settings (`settings`)

Route `/dashboard/settings` · manifest status **counter** · measured 2026-09-20

Generated by `npm run fidelity:report` from what `npm run fidelity` measured. Do not edit by hand — re-run it.

## fidelity — desk

**The prototype renders 28 landmarks on this page. We render 24.**

| landmark | prototype | ours |
|---|---:|---:|
| `.btn` | 6 | 2 |
| `.btnrow` | 2 | 1 |
| `.kv` | 1 | 2 |
| `.sec` | 6 | 6 |
| `.sec__body` | 6 | 6 |
| `.sec__head` | 6 | 6 |
| `.tbl` | 1 | 1 |

Structure: 5 missing, 1 extra, 23 matched. Rendering (light): 0 property differences on the 23 landmarks present on both sides.

### Missing — the prototype has these and we do not

- `#10 .btn`
- `#24 .btnrow`
- `#25 .btn`
- `#26 .btn`
- `#27 .btn`

### Extra — we render these and the prototype does not

- `#16 .kv`

### Dark mode — asserted on its own terms, never against the prototype

98 of 169 elements paint a colour of their own; 137 were swept (every element that paints or carries text, not only the 24 landmarks — see the "FIX ROUND 1" note in landmarks.ts). Checked against 50 resolved `--ct-*` tokens. 0 defects.


## fidelity-mobile — phone

**The prototype renders 9 landmarks on this page. We render 13.**

| landmark | prototype | ours |
|---|---:|---:|
| `.kv` | 0 | 1 |
| `.mbtn` | 1 | 1 |
| `.mhead` | 1 | 1 |
| `.mlist` | 1 | 1 |
| `.sec` | 2 | 3 |
| `.sec__body` | 2 | 3 |
| `.sec__head` | 2 | 3 |

Structure: 0 missing, 4 extra, 9 matched. Rendering (light): 0 property differences on the 9 landmarks present on both sides.

### Extra — we render these and the prototype does not

- `#7 .kv`
- `#8 .sec`
- `#9 .sec__head`
- `#10 .sec__body`

### Dark mode — asserted on its own terms, never against the prototype

62 of 120 elements paint a colour of their own; 84 were swept (every element that paints or carries text, not only the 13 landmarks — see the "FIX ROUND 1" note in landmarks.ts). Checked against 50 resolved `--ct-*` tokens. 0 defects.

