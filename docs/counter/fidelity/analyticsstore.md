# Fidelity — Analytics (`analyticsstore`)

Route `/dashboard/analytics/cmexd4zia0001jr04ljkdt9na` · manifest status **counter** · measured 2026-09-01

Generated by `npm run fidelity:report` from what `npm run fidelity` measured. Do not edit by hand — re-run it.

## fidelity — desk

**The prototype renders 32 landmarks on this page. We render 32.**

| landmark | prototype | ours |
|---|---:|---:|
| `.btn` | 1 | 1 |
| `.ch` | 3 | 3 |
| `.moneyline` | 6 | 6 |
| `.sec` | 7 | 7 |
| `.sec__body` | 4 | 4 |
| `.sec__head` | 7 | 7 |
| `.strip` | 1 | 1 |
| `.tbl` | 3 | 3 |

Structure: 0 missing, 0 extra, 32 matched. Rendering (light): 0 property differences on the 32 landmarks present on both sides.

### Dark mode — asserted on its own terms, never against the prototype

275 of 636 elements paint a colour of their own; 464 were swept (every element that paints or carries text, not only the 32 landmarks — see the "FIX ROUND 1" note in landmarks.ts). Checked against 47 resolved `--ct-*` tokens. 0 defects.


## fidelity-mobile — phone

**The prototype renders 9 landmarks on this page. We render 9.**

| landmark | prototype | ours |
|---|---:|---:|
| `.ch` | 1 | 1 |
| `.mlist` | 1 | 1 |
| `.mstrip` | 1 | 1 |
| `.sec` | 2 | 2 |
| `.sec__body` | 2 | 2 |
| `.sec__head` | 2 | 2 |

Structure: 0 missing, 0 extra, 9 matched. Rendering (light): 0 property differences on the 9 landmarks present on both sides.

### Dark mode — asserted on its own terms, never against the prototype

26 of 84 elements paint a colour of their own; 55 were swept (every element that paints or carries text, not only the 9 landmarks — see the "FIX ROUND 1" note in landmarks.ts). Checked against 47 resolved `--ct-*` tokens. 0 defects.

