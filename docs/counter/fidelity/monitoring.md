# Fidelity — Monitoring (`monitoring`)

Route `/dashboard/admin/monitoring` · manifest status **counter** · measured 2026-09-20

Generated by `npm run fidelity:report` from what `npm run fidelity` measured. Do not edit by hand — re-run it.

## fidelity — desk

**The prototype renders 13 landmarks on this page. We render 7.**

| landmark | prototype | ours |
|---|---:|---:|
| `.btn` | 1 | 2 |
| `.btnrow` | 0 | 1 |
| `.ch` | 1 | 0 |
| `.empty` | 0 | 1 |
| `.mlist` | 1 | 0 |
| `.sec` | 3 | 1 |
| `.sec__body` | 2 | 1 |
| `.sec__head` | 3 | 1 |
| `.strip` | 1 | 0 |
| `.tbl` | 1 | 0 |

Structure: 9 missing, 3 extra, 4 matched. Rendering (light): 0 property differences on the 4 landmarks present on both sides.

### Missing — the prototype has these and we do not

- `#1 .strip`
- `#2 .sec`
- `#3 .sec__head`
- `#4 .tbl`
- `#5 .sec`
- `#6 .sec__head`
- `#7 .sec__body`
- `#8 .ch`
- `#12 .mlist`

### Extra — we render these and the prototype does not

- `#0 .empty`
- `#1 .btnrow`
- `#3 .btn`

### Dark mode — asserted on its own terms, never against the prototype

9 of 14 elements paint a colour of their own; 13 were swept (every element that paints or carries text, not only the 7 landmarks — see the "FIX ROUND 1" note in landmarks.ts). Checked against 50 resolved `--ct-*` tokens. 0 defects.


## fidelity-mobile — phone

**The prototype renders 5 landmarks on this page. We render 2.**

| landmark | prototype | ours |
|---|---:|---:|
| `.empty` | 0 | 1 |
| `.mbtn` | 0 | 1 |
| `.mlist` | 1 | 0 |
| `.mstrip` | 1 | 0 |
| `.sec` | 1 | 0 |
| `.sec__body` | 1 | 0 |
| `.sec__head` | 1 | 0 |

Structure: 5 missing, 2 extra, 0 matched. Rendering was not compared: the two sides have no landmark in common, so there is nothing here to be right or wrong about. "0 rendering differences" would be a lie, and the gated rendering pass fails outright on this.

### Missing — the prototype has these and we do not

- `#0 .mstrip`
- `#1 .sec`
- `#2 .sec__head`
- `#3 .sec__body`
- `#4 .mlist`

### Extra — we render these and the prototype does not

- `#0 .empty`
- `#1 .mbtn`

### Dark mode — asserted on its own terms, never against the prototype

3 of 5 elements paint a colour of their own; 4 were swept (every element that paints or carries text, not only the 2 landmarks — see the "FIX ROUND 1" note in landmarks.ts). Checked against 50 resolved `--ct-*` tokens. 0 defects.

