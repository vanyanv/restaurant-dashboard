# Fidelity — Needs you (`alerts`)

Route `/dashboard/alerts` · manifest status **counter** · measured 2026-08-28

Generated by `npm run fidelity:report` from what `npm run fidelity` measured. Do not edit by hand — re-run it.

## fidelity — desk

**The prototype renders 8 landmarks on this page. We render 7.**

| landmark | prototype | ours |
|---|---:|---:|
| `.btn` | 1 | 0 |
| `.ch` | 1 | 1 |
| `.sec` | 2 | 2 |
| `.sec__body` | 1 | 1 |
| `.sec__head` | 1 | 1 |
| `.strip` | 1 | 1 |
| `.tbl` | 1 | 1 |

Structure: 1 missing, 0 extra, 7 matched. Rendering (light): 0 property differences on the 7 landmarks present on both sides.

### Missing — the prototype has these and we do not

- `#0 .btn`

### Dark mode — asserted on its own terms, never against the prototype

654 of 833 elements paint a colour of their own; 701 were swept (every element that paints or carries text, not only the 7 landmarks — see the "FIX ROUND 1" note in landmarks.ts). Checked against 44 resolved `--ct-*` tokens. 0 defects.


## fidelity-mobile — phone

**The prototype renders 8 landmarks on this page. We render 8.**

| landmark | prototype | ours |
|---|---:|---:|
| `.mlist` | 2 | 2 |
| `.sec` | 2 | 2 |
| `.sec__body` | 2 | 2 |
| `.sec__head` | 2 | 2 |

Structure: 0 missing, 0 extra, 8 matched. Rendering (light): 0 property differences on the 8 landmarks present on both sides.

### Dark mode — asserted on its own terms, never against the prototype

117 of 167 elements paint a colour of their own; 134 were swept (every element that paints or carries text, not only the 8 landmarks — see the "FIX ROUND 1" note in landmarks.ts). Checked against 44 resolved `--ct-*` tokens. 0 defects.

