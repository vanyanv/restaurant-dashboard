# Fidelity — Stock counts (`counts`)

Route `/dashboard/operations/inventory/counts` · manifest status **counter** · measured 2026-09-20

Generated by `npm run fidelity:report` from what `npm run fidelity` measured. Do not edit by hand — re-run it.

## fidelity — desk

**The prototype renders 15 landmarks on this page. We render 11.**

| landmark | prototype | ours |
|---|---:|---:|
| `.btn` | 3 | 1 |
| `.btnrow` | 1 | 0 |
| `.ch` | 1 | 0 |
| `.sec` | 3 | 3 |
| `.sec__body` | 2 | 2 |
| `.sec__head` | 3 | 3 |
| `.strip` | 1 | 1 |
| `.tbl` | 1 | 1 |

Structure: 4 missing, 0 extra, 11 matched. Rendering (light): 0 property differences on the 11 landmarks present on both sides.

### Missing — the prototype has these and we do not

- `#8 .ch`
- `#12 .btnrow`
- `#13 .btn`
- `#14 .btn`

### Dark mode — asserted on its own terms, never against the prototype

134 of 186 elements paint a colour of their own; 153 were swept (every element that paints or carries text, not only the 11 landmarks — see the "FIX ROUND 1" note in landmarks.ts). Checked against 50 resolved `--ct-*` tokens. 0 defects.


## fidelity-mobile — phone

**The prototype renders 6 landmarks on this page. We render 6.**

| landmark | prototype | ours |
|---|---:|---:|
| `.mbtn` | 1 | 1 |
| `.mlist` | 1 | 1 |
| `.mstrip` | 1 | 1 |
| `.sec` | 1 | 1 |
| `.sec__body` | 1 | 1 |
| `.sec__head` | 1 | 1 |

Structure: 0 missing, 0 extra, 6 matched. Rendering (light): 0 property differences on the 6 landmarks present on both sides.

### Dark mode — asserted on its own terms, never against the prototype

23 of 37 elements paint a colour of their own; 31 were swept (every element that paints or carries text, not only the 6 landmarks — see the "FIX ROUND 1" note in landmarks.ts). Checked against 50 resolved `--ct-*` tokens. 0 defects.

