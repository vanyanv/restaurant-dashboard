# Fidelity — Labor (`labor`)

Route `/dashboard/labor` · manifest status **counter** · measured 2026-08-31

Generated by `npm run fidelity:report` from what `npm run fidelity` measured. Do not edit by hand — re-run it.

## fidelity — desk

**The prototype renders 31 landmarks on this page. We render 31.**

| landmark | prototype | ours |
|---|---:|---:|
| `.btn` | 1 | 1 |
| `.ch` | 3 | 3 |
| `.fig` | 1 | 1 |
| `.headline` | 1 | 1 |
| `.qitem` | 1 | 1 |
| `.queue` | 1 | 1 |
| `.say` | 1 | 1 |
| `.sec` | 7 | 7 |
| `.sec__body` | 5 | 5 |
| `.sec__head` | 7 | 7 |
| `.strip` | 1 | 1 |
| `.tbl` | 2 | 2 |

Structure: 0 missing, 0 extra, 31 matched. Rendering (light): 2 property differences on the 31 landmarks present on both sides.

### Rendering differences

| landmark | property | prototype | ours |
|---|---|---|---|
| `#4 .strip` | `data-n` | `6` | `5` |
| `#4 .strip` | `grid-template-columns` | `6 tracks` | `5 tracks` |

### Dark mode — asserted on its own terms, never against the prototype

296 of 631 elements paint a colour of their own; 465 were swept (every element that paints or carries text, not only the 31 landmarks — see the "FIX ROUND 1" note in landmarks.ts). Checked against 46 resolved `--ct-*` tokens. 0 defects.


## fidelity-mobile — phone

**The prototype renders 9 landmarks on this page. We render 9.**

| landmark | prototype | ours |
|---|---:|---:|
| `.ch` | 1 | 1 |
| `.mlist` | 1 | 1 |
| `.mstrip` | 1 | 1 |
| `.sec` | 2 | 2 |
| `.sec__body` | 2 | 2 |
| `.sec__head` | 2 | 2 |

Structure: 0 missing, 0 extra, 9 matched. Rendering (light): 0 property differences on the 9 landmarks present on both sides.

### Dark mode — asserted on its own terms, never against the prototype

117 of 206 elements paint a colour of their own; 159 were swept (every element that paints or carries text, not only the 9 landmarks — see the "FIX ROUND 1" note in landmarks.ts). Checked against 46 resolved `--ct-*` tokens. 0 defects.

