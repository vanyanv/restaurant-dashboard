# Fidelity — Price monitor (`prices`)

Route `/dashboard/ingredients/prices` · manifest status **counter** · measured 2026-09-20

Generated by `npm run fidelity:report` from what `npm run fidelity` measured. Do not edit by hand — re-run it.

## fidelity — desk

**The prototype renders 9 landmarks on this page. We render 7.**

| landmark | prototype | ours |
|---|---:|---:|
| `.btn` | 1 | 0 |
| `.ch` | 1 | 0 |
| `.empty` | 0 | 3 |
| `.sec` | 2 | 2 |
| `.sec__body` | 1 | 0 |
| `.sec__head` | 2 | 2 |
| `.strip` | 1 | 0 |
| `.tbl` | 1 | 0 |

Structure: 5 missing, 3 extra, 4 matched. Rendering (light): 0 property differences on the 4 landmarks present on both sides.

### Missing — the prototype has these and we do not

- `#0 .btn`
- `#1 .strip`
- `#4 .sec__body`
- `#5 .ch`
- `#8 .tbl`

### Extra — we render these and the prototype does not

- `#0 .empty`
- `#3 .empty`
- `#6 .empty`

### Dark mode — asserted on its own terms, never against the prototype

14 of 23 elements paint a colour of their own; 20 were swept (every element that paints or carries text, not only the 7 landmarks — see the "FIX ROUND 1" note in landmarks.ts). Checked against 50 resolved `--ct-*` tokens. 0 defects.


## fidelity-mobile — phone

**The prototype renders 5 landmarks on this page. We render 5.**

| landmark | prototype | ours |
|---|---:|---:|
| `.empty` | 0 | 3 |
| `.mlist` | 1 | 0 |
| `.mstrip` | 1 | 0 |
| `.sec` | 1 | 1 |
| `.sec__body` | 1 | 0 |
| `.sec__head` | 1 | 1 |

Structure: 3 missing, 3 extra, 2 matched. Rendering (light): 0 property differences on the 2 landmarks present on both sides.

### Missing — the prototype has these and we do not

- `#0 .mstrip`
- `#3 .sec__body`
- `#4 .mlist`

### Extra — we render these and the prototype does not

- `#0 .empty`
- `#1 .empty`
- `#4 .empty`

### Dark mode — asserted on its own terms, never against the prototype

11 of 18 elements paint a colour of their own; 16 were swept (every element that paints or carries text, not only the 5 landmarks — see the "FIX ROUND 1" note in landmarks.ts). Checked against 50 resolved `--ct-*` tokens. 0 defects.

