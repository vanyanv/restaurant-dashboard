# Fidelity — Operations (`operations`)

Route `/dashboard/operations` · manifest status **counter** · measured 2026-09-20

Generated by `npm run fidelity:report` from what `npm run fidelity` measured. Do not edit by hand — re-run it.

## fidelity — desk

**The prototype renders 13 landmarks on this page. We render 11.**

| landmark | prototype | ours |
|---|---:|---:|
| `.btn` | 1 | 1 |
| `.qitem` | 4 | 2 |
| `.queue` | 1 | 1 |
| `.sec` | 2 | 2 |
| `.sec__body` | 1 | 1 |
| `.sec__head` | 2 | 2 |
| `.strip` | 1 | 1 |
| `.tbl` | 1 | 1 |

Structure: 2 missing, 0 extra, 11 matched. Rendering (light): 0 property differences on the 11 landmarks present on both sides.

### Missing — the prototype has these and we do not

- `#8 .qitem`
- `#9 .qitem`

### Dark mode — asserted on its own terms, never against the prototype

153 of 209 elements paint a colour of their own; 169 were swept (every element that paints or carries text, not only the 11 landmarks — see the "FIX ROUND 1" note in landmarks.ts). Checked against 50 resolved `--ct-*` tokens. 0 defects.


## fidelity-mobile — phone

**The prototype renders 4 landmarks on this page. We render 4.**

| landmark | prototype | ours |
|---|---:|---:|
| `.mlist` | 1 | 1 |
| `.sec` | 1 | 1 |
| `.sec__body` | 1 | 1 |
| `.sec__head` | 1 | 1 |

Structure: 0 missing, 0 extra, 4 matched. Rendering (light): 0 property differences on the 4 landmarks present on both sides.

### Dark mode — asserted on its own terms, never against the prototype

23 of 46 elements paint a colour of their own; 37 were swept (every element that paints or carries text, not only the 4 landmarks — see the "FIX ROUND 1" note in landmarks.ts). Checked against 50 resolved `--ct-*` tokens. 0 defects.

