# Fidelity — Operations (`operations`)

Route `/dashboard/operations` · manifest status **counter** · measured 2026-08-31

Generated by `npm run fidelity:report` from what `npm run fidelity` measured. Do not edit by hand — re-run it.

## fidelity — desk

**The prototype renders 13 landmarks on this page. We render 13.**

| landmark | prototype | ours |
|---|---:|---:|
| `.btn` | 1 | 1 |
| `.qitem` | 4 | 4 |
| `.queue` | 1 | 1 |
| `.sec` | 2 | 2 |
| `.sec__body` | 1 | 1 |
| `.sec__head` | 2 | 2 |
| `.strip` | 1 | 1 |
| `.tbl` | 1 | 1 |

Structure: 0 missing, 0 extra, 13 matched. Rendering (light): 0 property differences on the 13 landmarks present on both sides.

### Dark mode — asserted on its own terms, never against the prototype

163 of 223 elements paint a colour of their own; 181 were swept (every element that paints or carries text, not only the 13 landmarks — see the "FIX ROUND 1" note in landmarks.ts). Checked against 46 resolved `--ct-*` tokens. 0 defects.


## fidelity-mobile — phone

**The prototype renders 4 landmarks on this page. We render 4.**

| landmark | prototype | ours |
|---|---:|---:|
| `.mlist` | 1 | 1 |
| `.sec` | 1 | 1 |
| `.sec__body` | 1 | 1 |
| `.sec__head` | 1 | 1 |

Structure: 0 missing, 0 extra, 4 matched. Rendering (light): 0 property differences on the 4 landmarks present on both sides.

### Dark mode — asserted on its own terms, never against the prototype

110 of 155 elements paint a colour of their own; 131 were swept (every element that paints or carries text, not only the 4 landmarks — see the "FIX ROUND 1" note in landmarks.ts). Checked against 46 resolved `--ct-*` tokens. 0 defects.

