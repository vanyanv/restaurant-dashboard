# Fidelity — Orders (`orders`)

Route `/dashboard/orders` · manifest status **counter** · measured 2026-09-01

Generated by `npm run fidelity:report` from what `npm run fidelity` measured. Do not edit by hand — re-run it.

## fidelity — desk

**The prototype renders 10 landmarks on this page. We render 10.**

| landmark | prototype | ours |
|---|---:|---:|
| `.btn` | 2 | 2 |
| `.btnrow` | 1 | 1 |
| `.ch` | 1 | 1 |
| `.sec` | 2 | 2 |
| `.sec__body` | 1 | 1 |
| `.sec__head` | 1 | 1 |
| `.strip` | 1 | 1 |
| `.tbl` | 1 | 1 |

Structure: 0 missing, 0 extra, 10 matched. Rendering (light): 0 property differences on the 10 landmarks present on both sides.

### Dark mode — asserted on its own terms, never against the prototype

574 of 866 elements paint a colour of their own; 712 were swept (every element that paints or carries text, not only the 10 landmarks — see the "FIX ROUND 1" note in landmarks.ts). Checked against 47 resolved `--ct-*` tokens. 0 defects.


## fidelity-mobile — phone

**The prototype renders 5 landmarks on this page. We render 5.**

| landmark | prototype | ours |
|---|---:|---:|
| `.mlist` | 1 | 1 |
| `.mstrip` | 1 | 1 |
| `.sec` | 1 | 1 |
| `.sec__body` | 1 | 1 |
| `.sec__head` | 1 | 1 |

Structure: 0 missing, 0 extra, 5 matched. Rendering (light): 0 property differences on the 5 landmarks present on both sides.

### Dark mode — asserted on its own terms, never against the prototype

29 of 55 elements paint a colour of their own; 45 were swept (every element that paints or carries text, not only the 5 landmarks — see the "FIX ROUND 1" note in landmarks.ts). Checked against 47 resolved `--ct-*` tokens. 0 defects.

