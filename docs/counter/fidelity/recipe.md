# Fidelity — Recipe (`recipe`)

Route `/dashboard/recipes/cmm48cpo6000voxu9lafghk62` · manifest status **counter** · measured 2026-09-01

Generated by `npm run fidelity:report` from what `npm run fidelity` measured. Do not edit by hand — re-run it.

## fidelity — desk

**The prototype renders 29 landmarks on this page. We render 26.**

| landmark | prototype | ours |
|---|---:|---:|
| `.btn` | 6 | 3 |
| `.btnrow` | 1 | 1 |
| `.ch` | 1 | 1 |
| `.moneyline` | 3 | 3 |
| `.sec` | 6 | 6 |
| `.sec__body` | 5 | 5 |
| `.sec__head` | 6 | 6 |
| `.strip` | 1 | 1 |

Structure: 3 missing, 0 extra, 26 matched. Rendering (light): 0 property differences on the 26 landmarks present on both sides.

### Missing — the prototype has these and we do not

- `#8 .btn`
- `#20 .btn`
- `#24 .btn`

### Dark mode — asserted on its own terms, never against the prototype

200 of 346 elements paint a colour of their own; 274 were swept (every element that paints or carries text, not only the 26 landmarks — see the "FIX ROUND 1" note in landmarks.ts). Checked against 47 resolved `--ct-*` tokens. 0 defects.


## fidelity-mobile — phone

**The prototype renders 10 landmarks on this page. We render 9.**

| landmark | prototype | ours |
|---|---:|---:|
| `.mbtn` | 2 | 1 |
| `.mlist` | 1 | 1 |
| `.mstrip` | 1 | 1 |
| `.sec` | 2 | 2 |
| `.sec__body` | 2 | 2 |
| `.sec__head` | 2 | 2 |

Structure: 1 missing, 0 extra, 9 matched. Rendering (light): 0 property differences on the 9 landmarks present on both sides.

### Missing — the prototype has these and we do not

- `#9 .mbtn`

### Dark mode — asserted on its own terms, never against the prototype

23 of 37 elements paint a colour of their own; 30 were swept (every element that paints or carries text, not only the 9 landmarks — see the "FIX ROUND 1" note in landmarks.ts). Checked against 47 resolved `--ct-*` tokens. 0 defects.

