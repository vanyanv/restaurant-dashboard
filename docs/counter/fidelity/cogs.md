# Fidelity — COGS (`cogs`)

Route `/dashboard/cogs` · manifest status **counter** · measured 2026-08-31

Generated by `npm run fidelity:report` from what `npm run fidelity` measured. Do not edit by hand — re-run it.

## fidelity — desk

**The prototype renders 15 landmarks on this page. We render 15.**

| landmark | prototype | ours |
|---|---:|---:|
| `.btn` | 1 | 1 |
| `.ch` | 1 | 1 |
| `.sec` | 4 | 4 |
| `.sec__body` | 2 | 2 |
| `.sec__head` | 4 | 4 |
| `.strip` | 1 | 1 |
| `.tbl` | 2 | 2 |

Structure: 0 missing, 0 extra, 15 matched. Rendering (light): 2 property differences on the 15 landmarks present on both sides.

### Rendering differences

| landmark | property | prototype | ours |
|---|---|---|---|
| `#1 .strip` | `data-n` | `4` | `3` |
| `#1 .strip` | `grid-template-columns` | `4 tracks` | `3 tracks` |

### Dark mode — asserted on its own terms, never against the prototype

195 of 340 elements paint a colour of their own; 256 were swept (every element that paints or carries text, not only the 15 landmarks — see the "FIX ROUND 1" note in landmarks.ts). Checked against 47 resolved `--ct-*` tokens. 0 defects.


## fidelity-mobile — phone

**The prototype renders 9 landmarks on this page. We render 9.**

| landmark | prototype | ours |
|---|---:|---:|
| `.ch` | 1 | 1 |
| `.mlist` | 1 | 1 |
| `.mstrip` | 1 | 1 |
| `.sec` | 2 | 2 |
| `.sec__body` | 2 | 2 |
| `.sec__head` | 2 | 2 |

Structure: 0 missing, 0 extra, 9 matched. Rendering (light): 0 property differences on the 9 landmarks present on both sides.

### Dark mode — asserted on its own terms, never against the prototype

115 of 192 elements paint a colour of their own; 149 were swept (every element that paints or carries text, not only the 9 landmarks — see the "FIX ROUND 1" note in landmarks.ts). Checked against 47 resolved `--ct-*` tokens. 0 defects.

