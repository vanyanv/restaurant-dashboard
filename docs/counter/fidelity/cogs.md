# Fidelity — COGS (`cogs`)

Route `/dashboard/cogs` · manifest status **counter** · measured 2026-09-20

Generated by `npm run fidelity:report` from what `npm run fidelity` measured. Do not edit by hand — re-run it.

## fidelity — desk

**The prototype renders 15 landmarks on this page. We render 15.**

| landmark | prototype | ours |
|---|---:|---:|
| `.btn` | 1 | 1 |
| `.ch` | 1 | 1 |
| `.sec` | 4 | 4 |
| `.sec__body` | 2 | 4 |
| `.sec__head` | 4 | 4 |
| `.strip` | 1 | 1 |
| `.tbl` | 2 | 0 |

Structure: 2 missing, 2 extra, 13 matched. Rendering (light): 2 property differences on the 13 landmarks present on both sides.

### Missing — the prototype has these and we do not

- `#8 .tbl`
- `#14 .tbl`

### Extra — we render these and the prototype does not

- `#8 .sec__body`
- `#14 .sec__body`

### Rendering differences

| landmark | property | prototype | ours |
|---|---|---|---|
| `#1 .strip` | `data-n` | `4` | `3` |
| `#1 .strip` | `grid-template-columns` | `4 tracks` | `3 tracks` |

### Dark mode — asserted on its own terms, never against the prototype

135 of 240 elements paint a colour of their own; 180 were swept (every element that paints or carries text, not only the 15 landmarks — see the "FIX ROUND 1" note in landmarks.ts). Checked against 50 resolved `--ct-*` tokens. 0 defects.


## fidelity-mobile — phone

**The prototype renders 9 landmarks on this page. We render 8.**

| landmark | prototype | ours |
|---|---:|---:|
| `.ch` | 1 | 1 |
| `.mlist` | 1 | 0 |
| `.mstrip` | 1 | 1 |
| `.sec` | 2 | 2 |
| `.sec__body` | 2 | 2 |
| `.sec__head` | 2 | 2 |

Structure: 1 missing, 0 extra, 8 matched. Rendering (light): 0 property differences on the 8 landmarks present on both sides.

### Missing — the prototype has these and we do not

- `#8 .mlist`

### Dark mode — asserted on its own terms, never against the prototype

23 of 71 elements paint a colour of their own; 44 were swept (every element that paints or carries text, not only the 8 landmarks — see the "FIX ROUND 1" note in landmarks.ts). Checked against 50 resolved `--ct-*` tokens. 0 defects.

